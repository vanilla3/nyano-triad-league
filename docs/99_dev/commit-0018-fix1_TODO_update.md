# commit-0018-fix1 TODO update（差分）

## Done
- on-chain 互換サブセットで `earthBoostEdges != 255` を受理しないよう TS参照エンジンを修正（parity 回復）

## Next
- on-chain 互換サブセットで `firstPlayer != playerA` の入力も throw する不変条件テストを追加（今回のチェックを“見える化”）
- Synergy(v2) を v1 から分離して段階導入（v2 ruleset/v2 vectors を新設）
