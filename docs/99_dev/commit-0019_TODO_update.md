# commit-0019 TODO update（差分）

## Done（commit-0019）
- on-chain subset の不変条件：firstPlayer は playerA 固定（firstPlayer=0 必須）を TS でテスト化

## Next（commit-0020）
- Synergy は v2 として分離して導入（v1 core+tactics は不変）
  - v2 ruleset / v2 vectors を新設
  - traitEffects を 1要素だけ段階導入し、ベクタで固定
