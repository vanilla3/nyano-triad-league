# commit-0017-fix1 TODO update（差分）

## Done
- Fever ignore warning mark テストの tiles 参照を engine の実データ構造に合わせて修正（r.tiles.A/B）

## Next
- v1 core+tactics の無効入力（earthBoostEdges!=255 を含む）を不変条件テスト化（throw/revert 期待）
- 次段の Synergy(v2) を仕様/ベクタ分離して段階導入
