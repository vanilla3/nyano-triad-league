# commit-0022 TODO update（差分）

## Done（commit-0022）
- League に `submitMatch`（engineId 自動選択）を追加
- 公式 ruleset package を生成するスクリプトを追加
- ruleset package の仕様/手順をドキュメント化

## Next（commit-0023）
- `rulesets/official_onchain_rulesets.json` を CI 生成物として固定するか検討（レポ内にコミット or リリース添付）
- v2 の “canonical ruleset bytes” を公開する方法（IPFS pin + configHash）を固める
- v3候補（Synergy 1要素だけ追加）を設計し、必ず shared vectors 化
