# commit-0023 TODO update（差分）

## Done（commit-0023）
- 公式 ruleset package（スナップショット）をレポに同梱し、計算結果一致をテストで固定
- ruleset 出力スクリプトを安定出力対応（timestamp無し）に改善

## Next（commit-0024）
- `uri` をコミュニティ合意の IPFS に確定する（pin + ruleset spec 文書）
- `configHash` の意味を拡張する場合の方針を決める（rulesetId と独立させるか）
- v3（Synergy 1要素追加）を設計し、v2 vectors と同様に shared vectors を先に作る
