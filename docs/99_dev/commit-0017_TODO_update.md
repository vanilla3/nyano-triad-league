# commit-0017 TODO update（差分）

## Done（commit-0017）
- Fever（comboCount>=5）の “次手：警戒マーク無視” を shared vectors + unit test で固定

## Next（commit-0018）
- v1 core+tactics の “無効入力” をもう1本だけ追加（例：earthBoostEdges が 0..3 を含む入力は revert/throw）
- Synergy を v2 として段階導入（Earth/Shadow/Forest を ruleset に追加し、vectors も v2 として分離）
