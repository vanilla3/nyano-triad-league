[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200

# Fix: TriadEngineV1.resolve still hits "stack too deep" on legacy pipeline.
# Enable IR pipeline with optimizer (solc recommendation).
via_ir = true
